<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a762map">

    <!-- 网络 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- 定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- NEW：相机（拍照打卡） -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".A762MapApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.A762Map">

        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="${AMAP_API_KEY}" />

        <service android:name="com.amap.api.location.APSService" />

        <!-- NEW：FileProvider（给 TakePicture 输出 Uri 用） -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.profile.UserDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.profile.FootprintActivity"
            android:exported="false" />
        <activity
            android:name=".ui.profile.YearReportActivity"
            android:exported="false" />
        <activity
            android:name=".ui.profile.ExportPdfActivity"
            android:exported="false" />

        <activity android:name=".ui.profile.LoginActivity" android:exported="false"/>
        <activity android:name=".ui.profile.RechargeActivity" android:exported="false"/>
        <activity android:name=".ui.profile.EditProfileActivity" android:exported="false"/>
        <activity android:name=".ui.profile.AdminUserManageActivity" android:exported="false"/>

    </application>
</manifest>
